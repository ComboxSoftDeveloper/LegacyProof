Пустой наследник
  ок   поле _impl читается: XoshiroImpl
  ок   у наследника другая реализация: Net5CompatDerivedImpl
  ок   наследник ничего не переопределяет

Числа у наследника
  ок   совпадают с базовым типом: 1000 чисел

Массив не того типа
  ок   string[] присваивается в object[]
  ок   запись строки проходит
  ок   запись числа не проходит
  ок   Span<object> по string[] не создаётся
  ок   Span<string> по string[] создаётся

Устройство матрицы
  ок   шестнадцать открытых полей: 16
  ок   все поля float
  ок   есть вложенный непубличный тип

СВЕРКА ПРОЙДЕНА
